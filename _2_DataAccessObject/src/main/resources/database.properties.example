# Database Configuration Example
# Copy this file to database.properties and fill in your credentials
# OR set the corresponding environment variables: DB_URL, DB_USERNAME, DB_PASSWORD

db.url=jdbc:mysql://localhost:3306/shop2
db.username=root
db.password=0000
