# Учебный проект по Java и Spring

Добро пожаловать в репозиторий учебных материалов по Java! Этот проект представляет собой комплексное руководство, охватывающее широкий спектр тем: от основ языка до продвинутых концепций и разработки на Spring Framework.

## 📚 Структура проекта

Проект разделен на несколько модулей, каждый из которых посвящен определенной области изучения.

### 1. `_1_JavaLearning` (Основы Java)
Этот модуль содержит примеры кода и упражнения по ключевым аспектам языка Java:

*   **OOP**: Объектно-ориентированное программирование (классы, объекты, наследование, полиморфизм).
*   **Collections**: Работа с коллекциями (`List`, `Set`, `Map`) и алгоритмы.
*   **Algorithms**: Реализация классических алгоритмов (сортировка, поиск).
*   **GoF Patterns**: Примеры реализации шаблонов проектирования (Design Patterns) "Банды четырех".
*   **Stream API**: Использование потоков данных для функциональной обработки коллекций.
*   **Exception**: Обработка исключений (`try-catch`, `custom exceptions`).
*   **Concurrency**: Многопоточное программирование (`Thread`, `Runnable`, синхронизация).
*   **Reflection API**: Работа с рефлексией (анализ классов во время выполнения).
*   **Input/Output (I/O)**: Работа с файлами и потоками ввода-вывода.
*   **RegEx**: Использование регулярных выражений.
*   **SQL**: Основы работы с базами данных (JDBC).

### 2. `_2_DataAccessObject` (DAO Pattern)
Демонстрация паттерна проектирования **Data Access Object (DAO)**:
*   Разделение логики доступа к данным от бизнес-логики.
*   Примеры реализации интерфейсов DAO.
*   Работа с сущностями (`Entity`) и моделями данных.

### 3. `_3_SpringLearn` (Введение в Spring)
Изучение основ Spring Framework:
*   **XML Configuration**: Конфигурация контекста Spring с использованием XML-файлов.
*   **Annotation Configuration**: Современный подход к конфигурации с использованием аннотаций (`@Component`, `@Autowired`, `@Configuration`).

### 4. `spring-boot-parent` (Spring Boot & Data Access)
Продвинутая работа с данными в экосистеме Spring Boot:
*   **DemoJpa**: Работа с реляционными базами данных через **Spring Data JPA** (Hibernate).
*   **DemoR2DBC**: Использование реактивного подхода для доступа к данным (**Spring Data R2DBC**).
*   **Performance Testing**: Сравнение производительности JPA и R2DBC (JMeter тест `TestR2dbcAndJpa.jmx`).

### 5. `JavaAdvanced` (Продвинутые темы)
Дополнительные примеры и эксперименты:
*   Сериализация и работа с JSON (`JsonService`).
*   Примеры работы с простыми объектами (`Animal`, `Cat`, `Dog`).

## 🛠 Технологии

В проекте используются следующие технологии и инструменты:

*   **Java 24**: Последняя версия языка (согласно `pom.xml`).
*   **Maven**: Система сборки и управления зависимостями.
*   **Spring Framework**: Core, Context, Beans.
*   **Spring Boot**: Упрощение настройки и запуска приложений.
*   **Spring Data JPA / R2DBC**: Абстракции для работы с базами данных.
*   **JDBC**: Базовый API для работы с БД.
*   **JUnit / TestNG**: (Предполагается) для модульного тестирования.

## 🚀 Как запустить

Для сборки проекта и запуска тестов убедитесь, что у вас установлены **JDK 24** и **Maven**.

1.  **Клонирование репозитория:**
    ```bash
    git clone https://github.com/your-username/java-learning-project.git
    cd java-learning-project
    ```

2.  **Сборка проекта:**
    ```bash
    mvn clean install
    ```

3.  **Запуск отдельных модулей:**
    Перейдите в директорию нужного модуля и выполните соответствующие команды (например, запуск `Main` класса через IDE или maven-плагин).

## 📝 Заметки

*   Проект использует модульную структуру Maven.
*   В модуле `spring-boot-parent` содержится тест производительности JMeter (`.jmx`), который можно открыть в Apache JMeter для анализа сравнения JPA и R2DBC.

---
**Автор:** (Ваше Имя/Контакты)
